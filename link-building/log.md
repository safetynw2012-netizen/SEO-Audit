# Link-building log

Newest entries at the bottom. Every session appends. Record what was
actually done and what was actually learned — not what was planned.

---

## 2026-08-10 — Workstream activated

**State:** Cold start. No prior link-building work exists in this repo
or in any earlier session; the branch, the directory, and this log are
all new as of today. Zero outreach sent. Zero links acquired.

**Done:**

- Stood up `link-building/` with playbook, prospecting method, outreach
  templates, and pipeline CSV.
- Ranked eight tactics against this site's actual situation (affiliate
  model, ~10–20 indexed URLs, no trust pages, no author attribution)
  rather than against a generic ecommerce baseline. Full reasoning in
  `playbook.md` §3.
- Set Gate 0 in `README.md`: no outreach until the trust pages and
  author attribution from audit §5.1/§5.2/§5.3 are actually published.

**Not done, and why:**

- **No prospect list.** `prospects.csv` holds a schema row only. This
  session had no verified access to the live web, and the source audit
  hit the same wall (audit §0 — egress blocked to the origin and to
  archive.org). Writing plausible-looking .gov URLs into the pipeline
  without opening one of them would have produced a list that looks
  like progress and isn't. `prospecting.md` carries the operator
  library instead; the first session with browser access runs it and
  fills the CSV with verified rows.
- **No outreach drafted against real prospects.** Blocked on the above,
  and on Gate 0 regardless.

**Baseline to capture before the first send** (so the workstream has a
zero to measure from — none of these are known yet):

- Referring domains, total.
- Referring domains to the `/blogs/guides/*` explainer set.
- Referral sessions, trailing 30 days.

Audit §9.10 already calls for an Ahrefs or Semrush account; that
subscription is the blocker on all three numbers.

**Next session should:**

1. Check Gate 0. If open, the highest-value work is closing it — the
   drafts are sitting in `fixes/wcsafety-com/pages/` waiting on real
   names, a real email, and a real mailing address.
2. If Gate 0 is closed: build T1 asset #1 (respirator standards
   comparison table), because T2 and T3 both pitch it.
3. Capture the three baseline numbers above.
